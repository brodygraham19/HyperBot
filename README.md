
# HyperBot V3 — Ultimate Safe Edition

**Features:** verification panel, ticket panel + transcripts, moderation suite, utilities, welcome+DM, rich logs, anti-spam (safe), invite/link blocker with gift-link allowlist, reaction role buttons, giveaways, rotating status.

## Quick Start
1. Edit `.env` (token + confirm IDs).
2. `pip install -r requirements.txt`
3. `python bot.py`

**Railway:** Start command `python bot.py` (Procfile included).
